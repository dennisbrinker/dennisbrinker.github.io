/* ---------------------

    COURSE CONFIG

--------------------- */

courseOptions.courseUID = "6E098585-2E70-4AA2-814C-7ED0D9E16385";
courseOptions.activityCode = "202512art-DisplayBasics";
courseOptions.coursetype = "article";


// Only needed if videos are being used
var videoQuizLockOut = false;

// Used only when the course has subtitles.
// True when there is only an en-US video, false when there are translated videos.
var englishVideoOnly = true;

// Used when there are translated videos for some languages but NOT all. 'englishVideoOnly' variable must be false. If all languages have translated videos, leave this array empty.
var translatedVidCCs = [];


courseOptions.databaseLocation = false;
courseOptions.scormLocation = true;
//courseOptions.quizButtonDisplay = false;
